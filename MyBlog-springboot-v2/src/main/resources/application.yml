spring:
  application:
    name: myblog-v2
  profiles:
    default: local

management:
  endpoints:
    web:
      exposure:
        include: health

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true

myblog:
  cors:
    allowed-origins:
      - http://localhost:5173
  security:
    jwt:
      issuer: myblog-v2
      secret: ${MYBLOG_JWT_SECRET}
      access-token-ttl: 15m
    public-endpoints:
      - method: GET
        path: /actuator/health
      - method: POST
        path: /api/auth/login
      - method: GET
        path: /api/categories
      - method: GET
        path: /api/tags
      - method: GET
        path: /api/tags/top
      - method: GET
        path: /api/articles
      - method: GET
        path: /api/articles/*
      - method: POST
        path: /api/articles/*/access
      - method: GET
        path: /api/categories/*/articles
      - method: GET
        path: /api/tags/*/articles
      - method: GET
        path: /api/comments
      - method: GET
        path: /api/comments/*/replies
      - method: GET
        path: /api/comments/top
