<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tyb.myblog.v2.common.infrastructure.mail.MailFailureLogMapper">

    <!-- 邮件日志只持久化失败记录，成功发送不入库，避免低价值数据长期膨胀。 -->
    <insert id="insertFailed"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO t_mail_log (
            to_email,
            template,
            subject,
            status,
            retry_count,
            error_message,
            provider_message_id,
            params_json,
            created_at
        )
        VALUES (
            #{toEmail},
            #{template},
            #{subject},
            #{status},
            #{retryCount},
            #{errorMessage},
            #{providerMessageId},
            #{paramsJson},
            #{createdAt}
        )
    </insert>
</mapper>
