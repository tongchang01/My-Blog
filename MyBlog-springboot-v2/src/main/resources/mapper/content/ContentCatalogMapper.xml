<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tyb.myblog.v2.content.infrastructure.persistence.mapper.ContentCatalogMapper">

    <!--
        查询前台分类目录摘要。
        旧库 t_article.is_delete = 0 表示文章未删除，status = 1 表示文章已发布；
        只有同时满足这两个条件的文章才计入前台可见文章数量。
    -->
    <select id="listCategorySummaries" resultType="com.tyb.myblog.v2.content.domain.CategorySummary">
        select c.id,
               c.category_name as name,
               count(a.id) as articleCount
        from t_category c
        left join t_article a
               on a.category_id = c.id
              and a.is_delete = 0
              and a.status = 1
        group by c.id, c.category_name
        order by c.id asc
    </select>

    <!--
        查询前台标签目录摘要。
        旧库通过 t_article_tag 维护文章和标签关系，只有未删除且已发布的文章计入标签文章数量。
    -->
    <select id="listTagSummaries" resultType="com.tyb.myblog.v2.content.domain.TagSummary">
        select t.id,
               t.tag_name as name,
               count(a.id) as articleCount
        from t_tag t
        left join t_article_tag at on at.tag_id = t.id
        left join t_article a
               on a.id = at.article_id
              and a.is_delete = 0
              and a.status = 1
        group by t.id, t.tag_name
        order by t.id asc
    </select>

    <!--
        查询前台热门标签。
        排序先按已发布文章数量倒序，再按标签 ID 正序，保证数量相同时结果稳定。
    -->
    <select id="listTopTagSummaries" resultType="com.tyb.myblog.v2.content.domain.TagSummary">
        select t.id,
               t.tag_name as name,
               count(a.id) as articleCount
        from t_tag t
        left join t_article_tag at on at.tag_id = t.id
        left join t_article a
               on a.id = at.article_id
              and a.is_delete = 0
              and a.status = 1
        group by t.id, t.tag_name
        order by articleCount desc, t.id asc
        limit #{limit}
    </select>

</mapper>
