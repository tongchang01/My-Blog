<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tyb.myblog.v2.system.infrastructure.persistence.mapper.FriendLinkMapper">

    <sql id="friendLinkColumns">
        id,
        name,
        url,
        avatar_url,
        description,
        sort_order,
        status,
        created_at,
        created_by,
        updated_at,
        updated_by,
        deleted,
        deleted_at,
        deleted_by
    </sql>

    <!-- 公开列表只读取可见且未软删除的友链。 -->
    <select id="selectPublicVisible"
            resultType="com.tyb.myblog.v2.system.infrastructure.persistence.entity.FriendLinkEntity">
        SELECT <include refid="friendLinkColumns"/>
        FROM t_friend_link
        WHERE deleted = 0
          AND status = 1
        ORDER BY sort_order ASC, id ASC
    </select>

    <!-- 后台分页包含显示和隐藏状态，但不包含软删除记录。 -->
    <select id="selectActivePage"
            resultType="com.tyb.myblog.v2.system.infrastructure.persistence.entity.FriendLinkEntity">
        SELECT <include refid="friendLinkColumns"/>
        FROM t_friend_link
        WHERE deleted = 0
        ORDER BY sort_order ASC, id ASC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countActive" resultType="long">
        SELECT COUNT(*)
        FROM t_friend_link
        WHERE deleted = 0
    </select>

    <select id="selectActiveById"
            resultType="com.tyb.myblog.v2.system.infrastructure.persistence.entity.FriendLinkEntity">
        SELECT <include refid="friendLinkColumns"/>
        FROM t_friend_link
        WHERE id = #{id}
          AND deleted = 0
    </select>

    <!-- 写操作前锁定 active 行，避免更新与软删除交叉覆盖。 -->
    <select id="selectActiveByIdForUpdate"
            resultType="com.tyb.myblog.v2.system.infrastructure.persistence.entity.FriendLinkEntity">
        SELECT <include refid="friendLinkColumns"/>
        FROM t_friend_link
        WHERE id = #{id}
          AND deleted = 0
        FOR UPDATE
    </select>

    <!-- 固定按 ID 升序加锁，降低批量排序与其他写操作的死锁风险。 -->
    <select id="selectActiveByIdsForUpdate"
            resultType="com.tyb.myblog.v2.system.infrastructure.persistence.entity.FriendLinkEntity">
        SELECT <include refid="friendLinkColumns"/>
        FROM t_friend_link
        WHERE deleted = 0
          AND id IN
        <foreach collection="ids"
                 item="id"
                 open="("
                 separator=","
                 close=")">
            #{id}
        </foreach>
        ORDER BY id ASC
        FOR UPDATE
    </select>

    <!-- PUT 完整覆盖全部业务字段，并显式刷新更新审计。 -->
    <update id="updateActive">
        UPDATE t_friend_link
        SET name = #{link.name},
            url = #{link.url},
            avatar_url = #{link.avatarUrl},
            description = #{link.description},
            sort_order = #{link.sortOrder},
            status = #{link.status},
            updated_at = #{updatedAt},
            updated_by = #{updatedBy}
        WHERE id = #{link.id}
          AND deleted = 0
    </update>

    <update id="updateStatus">
        UPDATE t_friend_link
        SET status = #{status},
            updated_at = #{updatedAt},
            updated_by = #{updatedBy}
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <update id="updateSortOrder">
        UPDATE t_friend_link
        SET sort_order = #{sortOrder},
            updated_at = #{updatedAt},
            updated_by = #{updatedBy}
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <!-- 软删除必须完整写入删除和更新审计，禁止调用通用 delete。 -->
    <update id="softDelete">
        UPDATE t_friend_link
        SET deleted = 1,
            deleted_at = #{deletedAt},
            deleted_by = #{deletedBy},
            updated_at = #{deletedAt},
            updated_by = #{deletedBy}
        WHERE id = #{id}
          AND deleted = 0
    </update>

</mapper>
