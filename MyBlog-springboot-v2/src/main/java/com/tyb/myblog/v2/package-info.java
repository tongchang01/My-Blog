/**
 * 后端 V2 应用根包。
 *
 * <p>该包下按模块化单体组织代码：跨模块通用能力及其基础设施放在 {@code common}，
 * identity、content、comment、system、stats 等业务模块直接挂在根包下，
 * 各业务模块的技术实现收敛在模块内部的 {@code infrastructure} 子包。</p>
 */
package com.tyb.myblog.v2;
