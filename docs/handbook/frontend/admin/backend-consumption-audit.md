# 管理端后端能力消费审查

> 状态：已完成，作为能力映射与回归约束保留
> 适用范围：`frontend/apps/admin/` 在**不修改后端接口、领域规则或样式体系**前提下的交互补齐
> 最后校准：2026-07-25
> 对应代码：`frontend/apps/admin/src/`、`MyBlog-springboot-v2/src/main/java/`
> 权威程度：管理端能力对齐与后续拆分的事实清单

## 1. 结论与边界

本次逐一比对了管理端 API 调用、页面入口和后端所有管理端控制器。结论如下：

1. 管理端的文章、分类、标签、评论、友链、附件、站点配置、账号资料、密码和统计写接口均已有前端入口；不存在需要为了“补齐”而新建管理端后端接口的缺口。
2. 文章列表筛选、统计日期区间、友链查询、各管理表单业务语义和操作反馈均已完成前端对齐。
3. 服务端状态机、全量写入、软删除、公开可见性和统计口径均已在操作前提示，并由对应表单、管理 hook 或页面测试覆盖。
4. 友链历史上的关键字和状态筛选曾是反向不一致；当前前端仅发送后端支持的 `page`、`size`。后续不得重新引入当前页本地过滤来冒充服务端分页筛选。

本清单只安排前端变更：复用现有 API、`use*` 管理函数、Element Plus 控件和现有 i18n 结构；不新增接口、不改变返回字段、不修改后端业务规则，也不包含管理端视觉重做。

## 2. 审查方法与判定标准

### 2.1 事实来源

- 后端路由与参数：各模块 `Admin*Controller`、`CurrentUserController`、`AuthController`。
- 服务端规则：对应 application service、domain model 与 request support；不能只依据 OpenAPI 注释推断。
- 前端实际消费：`frontend/apps/admin/src/api/`、`features/` 内的调用点与页面入口。仅存在于 API 封装或测试、没有页面调用的函数视为“未消费”。

### 2.2 标记

| 标记 | 含义 | 后续处理 |
| --- | --- | --- |
| 已消费 | 页面已按后端契约使用 | 仅补充必要的业务提示 |
| 部分消费 | 已调用同一接口，但遗漏后端已有字段或查询参数 | 只改前端模型、查询构造和控件 |
| 未消费但无需入口 | 详情与列表字段重复，新增页面不会带来用户价值 | 保留 API 封装，不为覆盖率强行加入口 |
| 反向不一致 | 前端声明/发送的能力不被后端支持 | 不做伪筛选；先删除错误预期或另开后端议题 |

## 3. 全量路由与页面入口矩阵

下表覆盖当前可由管理端调用的业务路由；`/api/admin/security-probe` 仅用于安全测试，不是产品功能，故不纳入后台菜单。

| 模块 | 后端能力 | 当前管理端消费 | 结论 |
| --- | --- | --- | --- |
| 文章 | `GET /api/admin/articles`，支持 `status`、`categoryId`、`tagId`、`titleKeyword`、`createdFrom/To`、`publishFrom/To`、分页 | 列表页已传全部查询参数 | 已消费；后续仅补筛选语义与时间口径提示 |
| 文章 | 详情、创建、完整更新、软删除、回收站、恢复 | 编辑器、列表、回收站均已调用 | 已消费；补状态与字段规则提示 |
| 分类 | 列表、详情、创建、完整更新、批量排序、引用保护删除 | 列表、创建/编辑、批量排序、删除已调用 | 已消费；详情 API 未被页面调用且列表已有完整字段，无需另加入口 |
| 标签 | 列表、详情、创建、完整更新、引用保护删除 | 列表、创建/编辑、删除已调用 | 已消费；详情 API 同上，无需另加入口 |
| 评论 | 列表筛选、通过、隐藏、恢复、回复、软删除 | 全部操作和所有列表参数均已调用 | 已消费；补评论目标、隐私字段和动作后果说明 |
| 友链 | 列表、详情、创建、完整更新、显示状态、批量排序、软删除 | 写操作和排序已调用；列表只发送 `page`、`size`；详情 API 未被页面调用 | 已消费；详情无需入口；不以本地筛选伪造后端分页结果 |
| 附件 | 活动/已删除分页、详情、上传、软删除、恢复 | 活动/回收站、上传、删除、恢复已调用 | 已消费；详情 API 与卡片字段重复，无需入口；补上传/去重/软删除语义 |
| 站点配置 | 完整读取、完整更新 | 已调用 | 已消费；补“全量覆盖”、语言回退和字段格式提示 |
| 当前账号 | 当前用户读取、资料部分更新、改密 | 已调用 | 已消费；补公开资料、格式规则、改密后全端失效提示 |
| 认证会话 | 登录、刷新、全端退出 | 已调用 | 已消费，无新增页面入口 |
| 统计 | `GET /api/admin/stats/dashboard?from&to` | 仪表盘已提供日期区间、快捷范围和参数校验 | 已消费；后续仅补 JST 与指标口径说明 |

## 4. 已完成的查询对齐与后续约束

以下任务均已在当前分支完成。保留其事实与约束，避免后续视觉改造回退查询语义。

### P0：已修正真实的查询语义

#### A-01 文章列表：已消费全部现有筛选参数

**完成证据**：`ArticleListFilters`、`buildArticleListParams` 和列表页已包含分类、标签、创建时间范围、发布时间范围；页面复用初始化时加载的分类与标签，并在提交前阻止反向时间范围。

**当前行为**：

- 筛选卡提供分类、标签、创建时间范围、发布时间范围；继续复用已加载的 `categories`、`tags`。
- 可选筛选仅在用户填入时进入请求；重置会清空全部字段并回到第 1 页，分页和刷新保留当前筛选。
- 时间使用既有 JST 显示约定并提交 ISO `LocalDateTime`；反向时间范围会在请求前阻止。

**不可做**：后端没有文章列表排序参数，不能增加“按评论数/更新时间排序”控件或在当前页本地排序后宣称它是查询结果。

**验证**：`query.ts` 和文章列表 hook 测试覆盖参数构造、筛选传递、重置和反向时间范围。

#### A-02 统计看板：已暴露日期区间

**完成证据**：仪表盘已提供自定义日期范围、近 7 天/近 30 天快捷项和重置；`useStatsDashboard` 会在请求前校验双端、顺序和最长 366 天。

**当前行为与仍需保留的语义**：

- 页面允许自定义范围和“近 7 天 / 近 30 天”快捷项；缺省请求遵循后端最近 30 个 JST 自然日。
- 自定义范围会校验“同时填写、开始不晚于结束、最多 366 天”。
- `todayPv`、`todayUv` 始终表示 JST 当天，即使查询范围为历史区间；顶部文章 `dailyUvSum` 是区间内逐日 UV 之和而非跨日去重访客数。视觉改造不得弱化这些说明。

**验证**：`query.ts`、hook 与页面测试覆盖 JST 快捷范围、合法/非法日期区间及筛选交互。

#### A-03 友链：已停止显示无效的服务端筛选预期

**完成证据**：`FriendLinkListFilters` 与管理 hook 现仅保留 `page`、`size`，页面已移除关键字与状态筛选控件。

**当前行为**：

- 不发送后端不支持的关键字/状态参数，不使用当前页本地过滤伪装为服务端筛选。
- 如产品确实需要全局友链筛选，另开后端任务，为查询契约、索引、分页语义和测试单独设计；不混入本轮。

**验证**：友链 API 与管理 hook 测试确认列表请求只含 `page`、`size`，页面不再出现无效筛选控件。

## 5. 已完成的表单与操作语义

### B-01 文章编辑器

- 草稿允许缺少标题、正文和分类；非草稿按后端状态规则显示并校验必填项，摘要始终可空。
- 定时发布明确使用 JST 且必须是未来时间；PASSWORD 创建、编辑留空保留和切换状态清空均有对应提示。
- 标签最多 20 个并去重；slug 本地规范化和校验；封面只能从活动附件选择。
- 首页槽位只对已发布文章开放，页面展示 1 个置顶、2 个推荐的配额；发生冲突时保留表单并提示先释放对应槽位，不在前端猜测实时余量。

### B-02 评论

- 留言板固定使用 `GUESTBOOK / 0`，选择留言板时不显示目标 ID，列表显示“留言板”。
- ADMIN 审核详情展示安全外链、IP、User-Agent、父评论和回复目标；DEMO 不获得这些隐私字段。
- 回复只允许活动且已通过的评论；通过、隐藏、软删除和恢复分别说明文章计数与留言板口径，恢复不会把隐藏评论重新公开。

### B-03 分类、标签与友链

- 分类和标签 slug 在创建时本地规范化与校验，编辑时锁定；引用保护、冲突原因和迁移文章要求在删除前后均有明确反馈。
- 分类和友链排序只提交当前已修改项，校验值域并说明服务端原子更新；友链单页最多 50 项，因此不会超过批量接口 100 项上限。
- 友链状态切换说明公开影响，名称、URL、头像、描述和排序字段按后端格式约束校验。

### B-04 附件

- 前端在请求前拒绝超过 10 MiB 的文件，同时明确服务端仍按真实内容校验。
- 上传提示说明 SHA-256 去重可能复用或恢复既有附件。
- 删除确认说明软删除不会立即删除物理文件，并提醒封面、Logo、头像等引用会在后续保存时失败。

### B-05 站点配置与账号资料

- 站点配置完整提交 14 个字段，并提示全量覆盖、刷新后编辑和三语逐字段回退。
- 标题、摘要、About Markdown、Logo/Favicon、Spotify ID 均在请求前按后端格式校验。
- 作者资料说明公开范围并校验昵称、邮箱、URL 和三语简介；修改密码校验长度、禁止复用当前密码，成功后退出本机并说明所有设备需要重新登录。

### B-06 上游模板收口

- 已删除顶栏假通知入口与演示数据，避免把上游样例伪装成真实评论、任务和发布通知。
- 403/404/500 路由继续承担错误恢复，但不再作为普通业务菜单展示。
- 固定首页标签由不存在的 `/welcome` 改为真实 `/dashboard`，并删除孤立 Welcome 页面和无引用的 `ReCol`。
- 首屏 HTML 标题、包仓库元数据改为 MyBlog；上游来源和许可证仍由 `frontend/apps/admin/UPSTREAM.md` 保留。
- 删除无引用的 `@pureadmin/descriptions`、`@faker-js/faker` 及对应注释和类型声明。仍在使用的布局、主题、搜索和 `pure*` 翻译键不为追求“零上游痕迹”而重写。

## 6. 不应为了覆盖率而实现的接口

以下 API 封装目前只在自身测试中使用，但列表响应已经包含页面需要的完整字段。新增详情页、弹窗或额外请求没有业务价值，应保留封装和测试，不新增交互：

- `GET /api/admin/categories/{id}`
- `GET /api/admin/tags/{id}`
- `GET /api/admin/friend-links/{id}`
- `GET /api/admin/attachments/{id}`

同理，后端没有支持的文章排序、友链全局筛选、附件按名称/类型查询、统计跨日唯一访客等能力，不得在前端制造“看起来可用”的开关或标签。需要这些能力时单独进行后端契约设计。

## 7. 实施与验证结果

查询正确性、文章和评论语义、管理表单语义及模板收口均已按独立提交完成。后续修改管理端时继续执行：

1. 受影响的表单、hook 和页面局部测试。
2. `pnpm --dir frontend/apps/admin test`。
3. `pnpm --dir frontend/apps/admin typecheck`。
4. `pnpm --dir frontend/apps/admin build` 与 `pnpm --dir frontend/apps/admin check:bundle-budget`。

## 8. 本轮明确未包含

- 后台视觉重做、布局、主题或组件库替换。
- 新增或修改后端 API、数据库、权限、统计口径、对象存储策略。
- 文章相邻导航、音乐播放器等已在开放问题中独立跟踪的产品增量；PASSWORD 文章解锁已由独立产品任务完成。
- 任何前端本地筛选/排序对服务端分页结果的伪替代。

## 9. 相关权威资料

- [文章接口](../../api/article.md)、[分类与标签接口](../../api/category-tag.md)、[评论接口](../../api/comment.md)
- [友链接口](../../api/friend-link.md)、[附件接口](../../api/attachment.md)、[站点配置接口](../../api/site-config.md)
- [认证与用户接口](../../api/auth.md)、[统计接口](../../api/stats.md)
- [管理端实现摘要](README.md)
