# 后台 Admin 联调状态

> 状态：当前有效
> 适用范围：V2 后台管理端
> 最后校准：2026-07-03
> 对应代码：`frontend/apps/admin/`
> 权威程度：后台完成度记录

## 校准来源

- 后台当前事实以 `E:\My-Blog\frontend\apps\admin/src/` 的前端集成分支源码为准。
- 本后端 worktree 中的 `frontend/apps/admin/` 只有工程外壳、依赖和构建产物，没有可用于校准的 `src/`。
- 历史计划、设计、验收和 QA 截图已归档到 `../../../archive/frontend-admin/`，不作为当前权威依据。

## 已完成

- 后台基础工程、三语、静态路由、Pinia、Axios、统一错误模型。
- 登录 `/api/auth/login` -> `/api/auth/me` 原子编排。
- access token 自动刷新、单飞 refresh、原请求最多重放一次、退出清理。
- ADMIN/DEMO 权限：前端改善体验，后端仍是最终安全边界。
- 仪表盘复用 `/api/admin/stats/dashboard` 展示真实统计。
- 文章管理：列表、新建、编辑、状态、Markdown 预览、本地草稿、回收站、软删除和恢复入口。
- 分类/标签管理：列表、筛选、新增、编辑、删除；分类支持排序，标签不排序。
- 友链管理：列表、筛选、新增、编辑、显示/隐藏、排序、删除和头像附件选择器。
- 评论管理：列表、目标类型/目标 ID/审核状态/关键词/已删除筛选，ADMIN 可审核通过、隐藏、软删除、恢复和回复，DEMO 只读。
- 站点配置与作者资料：站点配置、About Markdown、Logo/Favicon、Spotify playlist ID、当前用户资料维护。
- 附件管理：图片上传、列表、详情、复制 URL、打开公开地址；文章封面和站点图片可复用附件选择器。
- 统计仪表盘：复用 `/api/admin/stats/dashboard`，展示 PV/UV 指标、趋势折线图、TOP 文章柱状图和语言分布饼图。
- DEMO 敏感字段裁剪：后端已裁剪文章非公开正文、评论邮箱/IP/UA 和附件内部存储字段；前端只负责只读体验优化。

## 仍需跟踪

- 前端可继续优化 DEMO 禁用态、提示和空敏感字段展示，但不得作为安全边界。

## 对应 open issues

- O-002 DEMO 敏感字段裁剪边界：已关闭。
- O-012 统计 TOP 文章 ID 类型校准：已关闭。
